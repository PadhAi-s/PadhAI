// Built out in Phase 7 (Gemini AI Assistant) onward.
// Every function here calls a Supabase Edge Function (supabase/functions/*)
// via supabase.functions.invoke(...) — this file never touches the Gemini
// API key directly, and neither should any other frontend code.
//
// Planned exports: askAI(), generateSyllabus(), generateQuiz(), solveImage()
export {};
